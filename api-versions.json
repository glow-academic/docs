{
  "_comment": "Records which api + cli versions this docs revision documents. Static docs has no runtime compat enforcement — this is for the maintainer to bump when re-generating from a new openapi.json + cli spec.",
  "api": {
    "version": "1.0.0"
  },
  "cli": {
    "version": "1.0.0"
  }
}
