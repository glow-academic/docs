# `GET` `/userinfo`

Userinfo

UserInfo endpoint — returns user claims from access token.

## Response

```
{
  "type": "object",
  "additionalProperties": true,
  "title": "Response Userinfo Userinfo Get"
}
```
