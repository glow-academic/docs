# `glow simulations refresh`

Refresh Simulation configurations.

## Usage

```bash
glow simulations refresh --id <id>
```

> Wire call: `POST /simulation/refresh`. Run `glow simulations refresh --help` for the full flag list.
