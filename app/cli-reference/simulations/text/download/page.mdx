# `glow simulations text download`

Download text on Simulation configurations.

## Usage

```bash
glow simulations text download --id <id>
```

> Wire call: `POST /simulation/text_download`. Run `glow simulations text download --help` for the full flag list.
