# `glow simulations file download`

Download file on Simulation configurations.

## Usage

```bash
glow simulations file download --id <id>
```

> Wire call: `POST /simulation/file_download`. Run `glow simulations file download --help` for the full flag list.
