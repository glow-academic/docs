# `glow simulations delete`

Delete Simulation configurations.

## Usage

```bash
glow simulations delete --id <id>
```

> Wire call: `POST /simulation/delete`. Run `glow simulations delete --help` for the full flag list.
