# `glow simulations call download`

Download call on Simulation configurations.

## Usage

```bash
glow simulations call download --id <id>
```

> Wire call: `POST /simulation/call_download`. Run `glow simulations call download --help` for the full flag list.
