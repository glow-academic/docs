# `glow cohorts file download`

Download file on User cohorts.

## Usage

```bash
glow cohorts file download --id <id>
```

> Wire call: `POST /cohort/file_download`. Run `glow cohorts file download --help` for the full flag list.
