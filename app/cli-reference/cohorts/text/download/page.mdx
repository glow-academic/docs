# `glow cohorts text download`

Download text on User cohorts.

## Usage

```bash
glow cohorts text download --id <id>
```

> Wire call: `POST /cohort/text_download`. Run `glow cohorts text download --help` for the full flag list.
