# `glow cohorts call download`

Download call on User cohorts.

## Usage

```bash
glow cohorts call download --id <id>
```

> Wire call: `POST /cohort/call_download`. Run `glow cohorts call download --help` for the full flag list.
