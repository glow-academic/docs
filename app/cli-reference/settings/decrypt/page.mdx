# `glow settings decrypt`

Decrypt Instance settings.

## Usage

```bash
glow settings decrypt --id <id>
```

> Wire call: `POST /setting/decrypt`. Run `glow settings decrypt --help` for the full flag list.
