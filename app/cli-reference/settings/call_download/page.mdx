# `glow settings call_download`

Call Download Instance settings.

## Usage

```bash
glow settings call_download --id <id>
```

> Wire call: `POST /setting/call_download`. Run `glow settings call_download --help` for the full flag list.
