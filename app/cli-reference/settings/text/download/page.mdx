# `glow settings text download`

Download text on Instance settings.

## Usage

```bash
glow settings text download --id <id>
```

> Wire call: `POST /setting/text_download`. Run `glow settings text download --help` for the full flag list.
