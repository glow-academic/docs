# `glow settings file download`

Download file on Instance settings.

## Usage

```bash
glow settings file download --id <id>
```

> Wire call: `POST /setting/file_download`. Run `glow settings file download --help` for the full flag list.
