# `glow settings refresh`

Refresh Instance settings.

## Usage

```bash
glow settings refresh --id <id>
```

> Wire call: `POST /setting/refresh`. Run `glow settings refresh --help` for the full flag list.
