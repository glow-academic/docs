# `glow settings call download`

Download call on Instance settings.

## Usage

```bash
glow settings call download --id <id>
```

> Wire call: `POST /setting/call_download`. Run `glow settings call download --help` for the full flag list.
