# `glow agents call download`

Download call on AI agents.

## Usage

```bash
glow agents call download --id <id>
```

> Wire call: `POST /agent/call_download`. Run `glow agents call download --help` for the full flag list.
