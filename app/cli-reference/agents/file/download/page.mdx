# `glow agents file download`

Download file on AI agents.

## Usage

```bash
glow agents file download --id <id>
```

> Wire call: `POST /agent/file_download`. Run `glow agents file download --help` for the full flag list.
