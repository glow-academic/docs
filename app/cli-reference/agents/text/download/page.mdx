# `glow agents text download`

Download text on AI agents.

## Usage

```bash
glow agents text download --id <id>
```

> Wire call: `POST /agent/text_download`. Run `glow agents text download --help` for the full flag list.
