# `glow rubrics file_download`

File Download Grading rubrics.

## Usage

```bash
glow rubrics file_download --id <id>
```

> Wire call: `POST /rubric/file_download`. Run `glow rubrics file_download --help` for the full flag list.
