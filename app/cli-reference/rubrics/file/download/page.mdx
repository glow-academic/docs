# `glow rubrics file download`

Download file on Grading rubrics.

## Usage

```bash
glow rubrics file download --id <id>
```

> Wire call: `POST /rubric/file_download`. Run `glow rubrics file download --help` for the full flag list.
