# `glow rubrics call download`

Download call on Grading rubrics.

## Usage

```bash
glow rubrics call download --id <id>
```

> Wire call: `POST /rubric/call_download`. Run `glow rubrics call download --help` for the full flag list.
