# `glow rubrics text download`

Download text on Grading rubrics.

## Usage

```bash
glow rubrics text download --id <id>
```

> Wire call: `POST /rubric/text_download`. Run `glow rubrics text download --help` for the full flag list.
