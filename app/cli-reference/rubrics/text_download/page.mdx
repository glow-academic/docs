# `glow rubrics text_download`

Text Download Grading rubrics.

## Usage

```bash
glow rubrics text_download --id <id>
```

> Wire call: `POST /rubric/text_download`. Run `glow rubrics text_download --help` for the full flag list.
