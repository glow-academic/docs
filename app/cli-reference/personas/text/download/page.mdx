# `glow personas text download`

Download text on AI personas for simulation.

## Usage

```bash
glow personas text download --id <id>
```

> Wire call: `POST /persona/text_download`. Run `glow personas text download --help` for the full flag list.
