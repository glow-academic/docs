# `glow personas file download`

Download file on AI personas for simulation.

## Usage

```bash
glow personas file download --id <id>
```

> Wire call: `POST /persona/file_download`. Run `glow personas file download --help` for the full flag list.
