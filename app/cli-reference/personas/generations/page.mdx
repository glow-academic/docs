# `glow personas generations`

Generations AI personas for simulation.

## Usage

```bash
glow personas generations --id <id>
```

> Wire call: `POST /persona/generations`. Run `glow personas generations --help` for the full flag list.
