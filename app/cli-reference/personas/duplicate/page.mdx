# `glow personas duplicate`

Duplicate AI personas for simulation.

## Usage

```bash
glow personas duplicate --id <id>
```

> Wire call: `POST /persona/duplicate`. Run `glow personas duplicate --help` for the full flag list.
