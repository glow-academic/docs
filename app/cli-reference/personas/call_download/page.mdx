# `glow personas call_download`

Call Download AI personas for simulation.

## Usage

```bash
glow personas call_download --id <id>
```

> Wire call: `POST /persona/call_download`. Run `glow personas call_download --help` for the full flag list.
