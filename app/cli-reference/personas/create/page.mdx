# `glow personas create`

Create AI personas for simulation.

## Usage

```bash
glow personas create --body '\{...\}'
```

> Wire call: `POST /persona/create`. Run `glow personas create --help` for the full flag list.
