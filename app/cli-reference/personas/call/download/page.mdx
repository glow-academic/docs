# `glow personas call download`

Download call on AI personas for simulation.

## Usage

```bash
glow personas call download --id <id>
```

> Wire call: `POST /persona/call_download`. Run `glow personas call download --help` for the full flag list.
