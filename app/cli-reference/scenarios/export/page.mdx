# `glow scenarios export`

Export Simulation scenarios.

## Usage

```bash
glow scenarios export --id <id>
```

> Wire call: `POST /scenario/export`. Run `glow scenarios export --help` for the full flag list.
