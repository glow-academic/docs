# `glow scenarios duplicate`

Duplicate Simulation scenarios.

## Usage

```bash
glow scenarios duplicate --id <id>
```

> Wire call: `POST /scenario/duplicate`. Run `glow scenarios duplicate --help` for the full flag list.
