# `glow scenarios image download`

Download image on Simulation scenarios.

## Usage

```bash
glow scenarios image download --id <id>
```

> Wire call: `POST /scenario/image_download`. Run `glow scenarios image download --help` for the full flag list.
