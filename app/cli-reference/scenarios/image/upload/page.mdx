# `glow scenarios image upload`

Upload image on Simulation scenarios.

## Usage

```bash
glow scenarios image upload --body '\{...\}'
```

> Wire call: `POST /scenario/image_upload`. Run `glow scenarios image upload --help` for the full flag list.
