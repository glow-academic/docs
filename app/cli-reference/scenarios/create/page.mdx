# `glow scenarios create`

Create Simulation scenarios.

## Usage

```bash
glow scenarios create --body '\{...\}'
```

> Wire call: `POST /scenario/create`. Run `glow scenarios create --help` for the full flag list.
