# `glow scenarios call download`

Download call on Simulation scenarios.

## Usage

```bash
glow scenarios call download --id <id>
```

> Wire call: `POST /scenario/call_download`. Run `glow scenarios call download --help` for the full flag list.
