# `glow scenarios watch`

Watch Simulation scenarios.

## Usage

```bash
glow scenarios watch --id <id>
```

> Wire call: `POST /scenario/watch`. Run `glow scenarios watch --help` for the full flag list.
