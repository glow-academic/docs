# `glow scenarios file preview`

Preview file on Simulation scenarios.

## Usage

```bash
glow scenarios file preview --id <id>
```

> Wire call: `POST /scenario/file_preview`. Run `glow scenarios file preview --help` for the full flag list.
