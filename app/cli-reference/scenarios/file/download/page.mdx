# `glow scenarios file download`

Download file on Simulation scenarios.

## Usage

```bash
glow scenarios file download --id <id>
```

> Wire call: `POST /scenario/file_download`. Run `glow scenarios file download --help` for the full flag list.
