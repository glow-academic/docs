# `glow scenarios text download`

Download text on Simulation scenarios.

## Usage

```bash
glow scenarios text download --id <id>
```

> Wire call: `POST /scenario/text_download`. Run `glow scenarios text download --help` for the full flag list.
