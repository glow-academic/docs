# `glow scenarios video download`

Download video on Simulation scenarios.

## Usage

```bash
glow scenarios video download --id <id>
```

> Wire call: `POST /scenario/video_download`. Run `glow scenarios video download --help` for the full flag list.
