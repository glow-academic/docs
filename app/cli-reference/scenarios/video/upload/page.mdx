# `glow scenarios video upload`

Upload video on Simulation scenarios.

## Usage

```bash
glow scenarios video upload --body '\{...\}'
```

> Wire call: `POST /scenario/video_upload`. Run `glow scenarios video upload --help` for the full flag list.
