# `glow providers text download`

Download text on AI provider integrations.

## Usage

```bash
glow providers text download --id <id>
```

> Wire call: `POST /provider/text_download`. Run `glow providers text download --help` for the full flag list.
