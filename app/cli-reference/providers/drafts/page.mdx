# `glow providers drafts`

Drafts AI provider integrations.

## Usage

```bash
glow providers drafts --id <id>
```

> Wire call: `POST /provider/drafts`. Run `glow providers drafts --help` for the full flag list.
