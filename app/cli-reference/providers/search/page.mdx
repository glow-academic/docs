# `glow providers search`

Search AI provider integrations.

## Usage

```bash
glow providers search
```

> Wire call: `POST /provider/search`. Run `glow providers search --help` for the full flag list.
