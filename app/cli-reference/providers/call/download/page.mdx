# `glow providers call download`

Download call on AI provider integrations.

## Usage

```bash
glow providers call download --id <id>
```

> Wire call: `POST /provider/call_download`. Run `glow providers call download --help` for the full flag list.
