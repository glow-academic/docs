# `glow providers file download`

Download file on AI provider integrations.

## Usage

```bash
glow providers file download --id <id>
```

> Wire call: `POST /provider/file_download`. Run `glow providers file download --help` for the full flag list.
