# `glow providers text_download`

Text Download AI provider integrations.

## Usage

```bash
glow providers text_download --id <id>
```

> Wire call: `POST /provider/text_download`. Run `glow providers text_download --help` for the full flag list.
