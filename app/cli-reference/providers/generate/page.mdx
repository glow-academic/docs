# `glow providers generate`

Generate AI provider integrations.

## Usage

```bash
glow providers generate --body '\{...\}'
```

> Wire call: `POST /provider/generate`. Run `glow providers generate --help` for the full flag list.
