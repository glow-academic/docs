# `glow providers file_download`

File Download AI provider integrations.

## Usage

```bash
glow providers file_download --id <id>
```

> Wire call: `POST /provider/file_download`. Run `glow providers file_download --help` for the full flag list.
