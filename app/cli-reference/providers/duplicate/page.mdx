# `glow providers duplicate`

Duplicate AI provider integrations.

## Usage

```bash
glow providers duplicate --id <id>
```

> Wire call: `POST /provider/duplicate`. Run `glow providers duplicate --help` for the full flag list.
