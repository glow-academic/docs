# `glow providers export`

Export AI provider integrations.

## Usage

```bash
glow providers export --id <id>
```

> Wire call: `POST /provider/export`. Run `glow providers export --help` for the full flag list.
