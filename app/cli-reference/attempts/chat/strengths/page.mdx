# `glow attempts chat strengths`

Strengths on chat (under attempts).

## Usage

```bash
glow attempts chat strengths --id <id>
```

> Wire call: `POST /attempt/chat_strengths`. Run `glow attempts chat strengths --help` for the full flag list.
