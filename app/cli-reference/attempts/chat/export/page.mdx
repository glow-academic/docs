# `glow attempts chat export`

Export on chat (under attempts).

## Usage

```bash
glow attempts chat export --id <id>
```

> Wire call: `POST /attempt/chat_export`. Run `glow attempts chat export --help` for the full flag list.
