# `glow attempts chat complete`

Complete on chat (under attempts).

## Usage

```bash
glow attempts chat complete --id <id>
```

> Wire call: `POST /attempt/chat_complete`. Run `glow attempts chat complete --help` for the full flag list.
