# `glow attempts chat response`

Response on chat (under attempts).

## Usage

```bash
glow attempts chat response --id <id>
```

> Wire call: `POST /attempt/chat_response`. Run `glow attempts chat response --help` for the full flag list.
