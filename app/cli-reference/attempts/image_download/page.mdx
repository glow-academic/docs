# `glow attempts image_download`

Image Download Simulation attempts.

## Usage

```bash
glow attempts image_download --id <id>
```

> Wire call: `POST /attempt/image_download`. Run `glow attempts image_download --help` for the full flag list.
