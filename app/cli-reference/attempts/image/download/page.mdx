# `glow attempts image download`

Download image on attempts.

## Usage

```bash
glow attempts image download --id <id>
```

> Wire call: `POST /attempt/image_download`. Run `glow attempts image download --help` for the full flag list.
