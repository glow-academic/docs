# `glow attempts call download`

Download call on attempts.

## Usage

```bash
glow attempts call download --id <id>
```

> Wire call: `POST /attempt/call_download`. Run `glow attempts call download --help` for the full flag list.
