# `glow attempts video download`

Download video on attempts.

## Usage

```bash
glow attempts video download --id <id>
```

> Wire call: `POST /attempt/video_download`. Run `glow attempts video download --help` for the full flag list.
