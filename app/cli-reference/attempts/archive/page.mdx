# `glow attempts archive`

Archive on attempts.

## Usage

```bash
glow attempts archive --id <id>
```

> Wire call: `POST /attempt/archive`. Run `glow attempts archive --help` for the full flag list.
