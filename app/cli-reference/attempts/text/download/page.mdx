# `glow attempts text download`

Download text on attempts.

## Usage

```bash
glow attempts text download --id <id>
```

> Wire call: `POST /attempt/text_download`. Run `glow attempts text download --help` for the full flag list.
