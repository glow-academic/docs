# `glow attempts dashboard`

Dashboard Simulation attempts.

## Usage

```bash
glow attempts dashboard --id <id>
```

> Wire call: `POST /attempt/dashboard`. Run `glow attempts dashboard --help` for the full flag list.
