# `glow attempts chat_grade`

Chat Grade Simulation attempts.

## Usage

```bash
glow attempts chat_grade --id <id>
```

> Wire call: `POST /attempt/chat_grade`. Run `glow attempts chat_grade --help` for the full flag list.
