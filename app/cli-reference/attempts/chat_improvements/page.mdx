# `glow attempts chat_improvements`

Chat Improvements Simulation attempts.

## Usage

```bash
glow attempts chat_improvements --id <id>
```

> Wire call: `POST /attempt/chat_improvements`. Run `glow attempts chat_improvements --help` for the full flag list.
