# `glow attempts chat_complete`

Chat Complete Simulation attempts.

## Usage

```bash
glow attempts chat_complete --id <id>
```

> Wire call: `POST /attempt/chat_complete`. Run `glow attempts chat_complete --help` for the full flag list.
