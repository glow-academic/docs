# `glow attempts audio upload`

Upload audio on attempts.

## Usage

```bash
glow attempts audio upload --body '\{...\}'
```

> Wire call: `POST /attempt/audio_upload`. Run `glow attempts audio upload --help` for the full flag list.
