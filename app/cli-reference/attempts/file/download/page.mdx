# `glow attempts file download`

Download file on attempts.

## Usage

```bash
glow attempts file download --id <id>
```

> Wire call: `POST /attempt/file_download`. Run `glow attempts file download --help` for the full flag list.
