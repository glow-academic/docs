# `glow attempts file preview`

Preview file on attempts.

## Usage

```bash
glow attempts file preview --id <id>
```

> Wire call: `POST /attempt/file_preview`. Run `glow attempts file preview --help` for the full flag list.
