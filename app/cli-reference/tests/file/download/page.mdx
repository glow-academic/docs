# `glow tests file download`

Download file on tests.

## Usage

```bash
glow tests file download --id <id>
```

> Wire call: `POST /test/file_download`. Run `glow tests file download --help` for the full flag list.
