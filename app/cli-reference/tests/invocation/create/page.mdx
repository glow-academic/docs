# `glow tests invocation create`

Create on invocation (under tests).

## Usage

```bash
glow tests invocation create --body '\{...\}'
```

> Wire call: `POST /test/invocation_create`. Run `glow tests invocation create --help` for the full flag list.
