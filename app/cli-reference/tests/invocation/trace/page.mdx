# `glow tests invocation trace`

Trace on invocation (under tests).

## Usage

```bash
glow tests invocation trace --id <id>
```

> Wire call: `POST /test/invocation_trace`. Run `glow tests invocation trace --help` for the full flag list.
