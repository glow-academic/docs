# `glow tests text download`

Download text on tests.

## Usage

```bash
glow tests text download --id <id>
```

> Wire call: `POST /test/text_download`. Run `glow tests text download --help` for the full flag list.
