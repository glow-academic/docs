# `glow tests call download`

Download call on tests.

## Usage

```bash
glow tests call download --id <id>
```

> Wire call: `POST /test/call_download`. Run `glow tests call download --help` for the full flag list.
