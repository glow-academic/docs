# `glow tests benchmark get`

Get on benchmark (under tests).

## Usage

```bash
glow tests benchmark get --id <id>
```

> Wire call: `POST /test/benchmark`. Run `glow tests benchmark get --help` for the full flag list.
