# `glow tools text download`

Download text on Tool integrations.

## Usage

```bash
glow tools text download --id <id>
```

> Wire call: `POST /tool/text_download`. Run `glow tools text download --help` for the full flag list.
