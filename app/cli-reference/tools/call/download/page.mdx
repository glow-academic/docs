# `glow tools call download`

Download call on Tool integrations.

## Usage

```bash
glow tools call download --id <id>
```

> Wire call: `POST /tool/call_download`. Run `glow tools call download --help` for the full flag list.
