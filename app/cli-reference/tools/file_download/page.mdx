# `glow tools file_download`

File Download Tool integrations.

## Usage

```bash
glow tools file_download --id <id>
```

> Wire call: `POST /tool/file_download`. Run `glow tools file_download --help` for the full flag list.
