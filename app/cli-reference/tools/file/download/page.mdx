# `glow tools file download`

Download file on Tool integrations.

## Usage

```bash
glow tools file download --id <id>
```

> Wire call: `POST /tool/file_download`. Run `glow tools file download --help` for the full flag list.
