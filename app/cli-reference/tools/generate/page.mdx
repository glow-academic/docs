# `glow tools generate`

Generate Tool integrations.

## Usage

```bash
glow tools generate --body '\{...\}'
```

> Wire call: `POST /tool/generate`. Run `glow tools generate --help` for the full flag list.
