# `glow departments text download`

Download text on Organizational departments.

## Usage

```bash
glow departments text download --id <id>
```

> Wire call: `POST /department/text_download`. Run `glow departments text download --help` for the full flag list.
