# `glow departments call_download`

Call Download Organizational departments.

## Usage

```bash
glow departments call_download --id <id>
```

> Wire call: `POST /department/call_download`. Run `glow departments call_download --help` for the full flag list.
