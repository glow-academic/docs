# `glow departments call download`

Download call on Organizational departments.

## Usage

```bash
glow departments call download --id <id>
```

> Wire call: `POST /department/call_download`. Run `glow departments call download --help` for the full flag list.
