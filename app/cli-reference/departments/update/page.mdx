# `glow departments update`

Update Organizational departments.

## Usage

```bash
glow departments update --id <id>
```

> Wire call: `POST /department/update`. Run `glow departments update --help` for the full flag list.
