# `glow departments file download`

Download file on Organizational departments.

## Usage

```bash
glow departments file download --id <id>
```

> Wire call: `POST /department/file_download`. Run `glow departments file download --help` for the full flag list.
