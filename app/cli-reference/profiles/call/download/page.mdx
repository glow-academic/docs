# `glow profiles call download`

Download call on User profiles.

## Usage

```bash
glow profiles call download --id <id>
```

> Wire call: `POST /profile/call_download`. Run `glow profiles call download --help` for the full flag list.
