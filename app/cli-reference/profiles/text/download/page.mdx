# `glow profiles text download`

Download text on User profiles.

## Usage

```bash
glow profiles text download --id <id>
```

> Wire call: `POST /profile/text_download`. Run `glow profiles text download --help` for the full flag list.
