# `glow profiles text_download`

Text Download User profiles.

## Usage

```bash
glow profiles text_download --id <id>
```

> Wire call: `POST /profile/text_download`. Run `glow profiles text_download --help` for the full flag list.
