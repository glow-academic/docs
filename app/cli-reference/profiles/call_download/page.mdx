# `glow profiles call_download`

Call Download User profiles.

## Usage

```bash
glow profiles call_download --id <id>
```

> Wire call: `POST /profile/call_download`. Run `glow profiles call_download --help` for the full flag list.
