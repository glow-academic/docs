# `glow profiles file download`

Download file on User profiles.

## Usage

```bash
glow profiles file download --id <id>
```

> Wire call: `POST /profile/file_download`. Run `glow profiles file download --help` for the full flag list.
