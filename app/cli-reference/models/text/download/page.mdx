# `glow models text download`

Download text on AI model configurations.

## Usage

```bash
glow models text download --id <id>
```

> Wire call: `POST /model/text_download`. Run `glow models text download --help` for the full flag list.
