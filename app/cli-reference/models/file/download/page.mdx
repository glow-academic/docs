# `glow models file download`

Download file on AI model configurations.

## Usage

```bash
glow models file download --id <id>
```

> Wire call: `POST /model/file_download`. Run `glow models file download --help` for the full flag list.
