# `glow models call download`

Download call on AI model configurations.

## Usage

```bash
glow models call download --id <id>
```

> Wire call: `POST /model/call_download`. Run `glow models call download --help` for the full flag list.
