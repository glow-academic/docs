# `glow models file_download`

File Download AI model configurations.

## Usage

```bash
glow models file_download --id <id>
```

> Wire call: `POST /model/file_download`. Run `glow models file_download --help` for the full flag list.
