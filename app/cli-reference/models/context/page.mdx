# `glow models context`

Context AI model configurations.

## Usage

```bash
glow models context --id <id>
```

> Wire call: `POST /model/context`. Run `glow models context --help` for the full flag list.
