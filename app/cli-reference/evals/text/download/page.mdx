# `glow evals text download`

Download text on Evaluation configurations.

## Usage

```bash
glow evals text download --id <id>
```

> Wire call: `POST /eval/text_download`. Run `glow evals text download --help` for the full flag list.
