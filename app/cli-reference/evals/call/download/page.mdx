# `glow evals call download`

Download call on Evaluation configurations.

## Usage

```bash
glow evals call download --id <id>
```

> Wire call: `POST /eval/call_download`. Run `glow evals call download --help` for the full flag list.
