# `glow evals file download`

Download file on Evaluation configurations.

## Usage

```bash
glow evals file download --id <id>
```

> Wire call: `POST /eval/file_download`. Run `glow evals file download --help` for the full flag list.
