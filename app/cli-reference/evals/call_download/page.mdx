# `glow evals call_download`

Call Download Evaluation configurations.

## Usage

```bash
glow evals call_download --id <id>
```

> Wire call: `POST /eval/call_download`. Run `glow evals call_download --help` for the full flag list.
