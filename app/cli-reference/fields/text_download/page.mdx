# `glow fields text_download`

Text Download Custom fields.

## Usage

```bash
glow fields text_download --id <id>
```

> Wire call: `POST /field/text_download`. Run `glow fields text_download --help` for the full flag list.
