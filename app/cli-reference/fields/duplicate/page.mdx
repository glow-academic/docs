# `glow fields duplicate`

Duplicate Custom fields.

## Usage

```bash
glow fields duplicate --id <id>
```

> Wire call: `POST /field/duplicate`. Run `glow fields duplicate --help` for the full flag list.
