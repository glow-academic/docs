# `glow fields call download`

Download call on Custom fields.

## Usage

```bash
glow fields call download --id <id>
```

> Wire call: `POST /field/call_download`. Run `glow fields call download --help` for the full flag list.
