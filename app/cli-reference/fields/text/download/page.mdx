# `glow fields text download`

Download text on Custom fields.

## Usage

```bash
glow fields text download --id <id>
```

> Wire call: `POST /field/text_download`. Run `glow fields text download --help` for the full flag list.
