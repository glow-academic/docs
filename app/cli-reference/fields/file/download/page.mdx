# `glow fields file download`

Download file on Custom fields.

## Usage

```bash
glow fields file download --id <id>
```

> Wire call: `POST /field/file_download`. Run `glow fields file download --help` for the full flag list.
