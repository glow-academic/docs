# `glow system image download`

Download image on system.

## Usage

```bash
glow system image download --id <id>
```

> Wire call: `POST /system/image_download`. Run `glow system image download --help` for the full flag list.
