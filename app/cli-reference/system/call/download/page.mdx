# `glow system call download`

Download call on system.

## Usage

```bash
glow system call download --id <id>
```

> Wire call: `POST /system/call_download`. Run `glow system call download --help` for the full flag list.
