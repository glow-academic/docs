# `glow system activity`

Activity System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system activity --id <id>
```

> Wire call: `POST /system/activity`. Run `glow system activity --help` for the full flag list.
