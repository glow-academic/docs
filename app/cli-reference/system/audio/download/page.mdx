# `glow system audio download`

Download audio on system.

## Usage

```bash
glow system audio download --id <id>
```

> Wire call: `POST /system/audio_download`. Run `glow system audio download --help` for the full flag list.
