# `glow system text download`

Download text on system.

## Usage

```bash
glow system text download --id <id>
```

> Wire call: `POST /system/text_download`. Run `glow system text download --help` for the full flag list.
