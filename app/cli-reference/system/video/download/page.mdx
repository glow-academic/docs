# `glow system video download`

Download video on system.

## Usage

```bash
glow system video download --id <id>
```

> Wire call: `POST /system/video_download`. Run `glow system video download --help` for the full flag list.
