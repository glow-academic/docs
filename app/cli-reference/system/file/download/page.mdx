# `glow system file download`

Download file on system.

## Usage

```bash
glow system file download --id <id>
```

> Wire call: `POST /system/file_download`. Run `glow system file download --help` for the full flag list.
