# `glow system file preview`

Preview file on system.

## Usage

```bash
glow system file preview --id <id>
```

> Wire call: `POST /system/file_preview`. Run `glow system file preview --help` for the full flag list.
