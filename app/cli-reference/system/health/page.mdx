# `glow system health`

Health System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system health --id <id>
```

> Wire call: `POST /system/health`. Run `glow system health --help` for the full flag list.
