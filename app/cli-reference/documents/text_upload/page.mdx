# `glow documents text_upload`

Text Upload Document management.

## Usage

```bash
glow documents text_upload --id <id>
```

> Wire call: `POST /document/text_upload`. Run `glow documents text_upload --help` for the full flag list.
