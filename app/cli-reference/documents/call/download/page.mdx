# `glow documents call download`

Download call on Document management.

## Usage

```bash
glow documents call download --id <id>
```

> Wire call: `POST /document/call_download`. Run `glow documents call download --help` for the full flag list.
