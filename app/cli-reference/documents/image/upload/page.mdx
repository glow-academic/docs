# `glow documents image upload`

Upload image on Document management.

## Usage

```bash
glow documents image upload --body '\{...\}'
```

> Wire call: `POST /document/image_upload`. Run `glow documents image upload --help` for the full flag list.
