# `glow documents image download`

Download image on Document management.

## Usage

```bash
glow documents image download --id <id>
```

> Wire call: `POST /document/image_download`. Run `glow documents image download --help` for the full flag list.
