# `glow documents file preview`

Preview file on Document management.

## Usage

```bash
glow documents file preview --id <id>
```

> Wire call: `POST /document/file_preview`. Run `glow documents file preview --help` for the full flag list.
