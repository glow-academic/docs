# `glow documents file download`

Download file on Document management.

## Usage

```bash
glow documents file download --id <id>
```

> Wire call: `POST /document/file_download`. Run `glow documents file download --help` for the full flag list.
