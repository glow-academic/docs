# `glow documents file upload`

Upload file on Document management.

## Usage

```bash
glow documents file upload --body '\{...\}'
```

> Wire call: `POST /document/file_upload`. Run `glow documents file upload --help` for the full flag list.
