# `glow documents text download`

Download text on Document management.

## Usage

```bash
glow documents text download --id <id>
```

> Wire call: `POST /document/text_download`. Run `glow documents text download --help` for the full flag list.
