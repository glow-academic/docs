# `glow documents text upload`

Upload text on Document management.

## Usage

```bash
glow documents text upload --body '\{...\}'
```

> Wire call: `POST /document/text_upload`. Run `glow documents text upload --help` for the full flag list.
