# `glow documents duplicate`

Duplicate Document management.

## Usage

```bash
glow documents duplicate --id <id>
```

> Wire call: `POST /document/duplicate`. Run `glow documents duplicate --help` for the full flag list.
