# `glow auths file download`

Download file on Authentication records.

## Usage

```bash
glow auths file download --id <id>
```

> Wire call: `POST /auth/file_download`. Run `glow auths file download --help` for the full flag list.
