# `glow auths text download`

Download text on Authentication records.

## Usage

```bash
glow auths text download --id <id>
```

> Wire call: `POST /auth/text_download`. Run `glow auths text download --help` for the full flag list.
