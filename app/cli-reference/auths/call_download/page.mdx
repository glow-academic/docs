# `glow auths call_download`

Call Download Authentication records.

## Usage

```bash
glow auths call_download --id <id>
```

> Wire call: `POST /auth/call_download`. Run `glow auths call_download --help` for the full flag list.
