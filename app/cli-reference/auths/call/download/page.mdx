# `glow auths call download`

Download call on Authentication records.

## Usage

```bash
glow auths call download --id <id>
```

> Wire call: `POST /auth/call_download`. Run `glow auths call download --help` for the full flag list.
