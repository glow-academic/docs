# `glow auths generate`

Generate Authentication records.

## Usage

```bash
glow auths generate --body '\{...\}'
```

> Wire call: `POST /auth/generate`. Run `glow auths generate --help` for the full flag list.
