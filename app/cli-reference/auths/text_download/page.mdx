# `glow auths text_download`

Text Download Authentication records.

## Usage

```bash
glow auths text_download --id <id>
```

> Wire call: `POST /auth/text_download`. Run `glow auths text_download --help` for the full flag list.
