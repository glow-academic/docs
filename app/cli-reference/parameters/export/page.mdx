# `glow parameters export`

Export Configuration parameters.

## Usage

```bash
glow parameters export --id <id>
```

> Wire call: `POST /parameter/export`. Run `glow parameters export --help` for the full flag list.
