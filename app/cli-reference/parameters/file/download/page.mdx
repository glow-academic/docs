# `glow parameters file download`

Download file on Configuration parameters.

## Usage

```bash
glow parameters file download --id <id>
```

> Wire call: `POST /parameter/file_download`. Run `glow parameters file download --help` for the full flag list.
