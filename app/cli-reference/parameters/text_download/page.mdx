# `glow parameters text_download`

Text Download Configuration parameters.

## Usage

```bash
glow parameters text_download --id <id>
```

> Wire call: `POST /parameter/text_download`. Run `glow parameters text_download --help` for the full flag list.
