# `glow parameters context`

Context Configuration parameters.

## Usage

```bash
glow parameters context --id <id>
```

> Wire call: `POST /parameter/context`. Run `glow parameters context --help` for the full flag list.
