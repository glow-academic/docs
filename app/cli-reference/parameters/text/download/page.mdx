# `glow parameters text download`

Download text on Configuration parameters.

## Usage

```bash
glow parameters text download --id <id>
```

> Wire call: `POST /parameter/text_download`. Run `glow parameters text download --help` for the full flag list.
