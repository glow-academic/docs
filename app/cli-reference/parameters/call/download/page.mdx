# `glow parameters call download`

Download call on Configuration parameters.

## Usage

```bash
glow parameters call download --id <id>
```

> Wire call: `POST /parameter/call_download`. Run `glow parameters call download --help` for the full flag list.
