# `glow auths refresh`

# `glow auths refresh`

Refresh Authentication records.

## Usage

```bash
glow auths refresh --id <id>
```

> Wire call: `POST /auth/refresh`. Run `glow auths refresh --help` for the full flag list.
