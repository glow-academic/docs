# `glow auths drafts`

# `glow auths drafts`

Drafts Authentication records.

## Usage

```bash
glow auths drafts --id <id>
```

> Wire call: `POST /auth/drafts`. Run `glow auths drafts --help` for the full flag list.
