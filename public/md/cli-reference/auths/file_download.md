# `glow auths file_download`

# `glow auths file_download`

File Download Authentication records.

## Usage

```bash
glow auths file_download --id <id>
```

> Wire call: `POST /auth/file_download`. Run `glow auths file_download --help` for the full flag list.
