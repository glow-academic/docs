# `glow auths duplicate`

# `glow auths duplicate`

Duplicate Authentication records.

## Usage

```bash
glow auths duplicate --id <id>
```

> Wire call: `POST /auth/duplicate`. Run `glow auths duplicate --help` for the full flag list.
