# `glow auths watch`

# `glow auths watch`

Watch Authentication records.

## Usage

```bash
glow auths watch --id <id>
```

> Wire call: `POST /auth/watch`. Run `glow auths watch --help` for the full flag list.
