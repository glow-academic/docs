# `glow auths context`

# `glow auths context`

Context Authentication records.

## Usage

```bash
glow auths context --id <id>
```

> Wire call: `POST /auth/context`. Run `glow auths context --help` for the full flag list.
