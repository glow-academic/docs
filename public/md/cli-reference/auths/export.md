# `glow auths export`

# `glow auths export`

Export Authentication records.

## Usage

```bash
glow auths export --id <id>
```

> Wire call: `POST /auth/export`. Run `glow auths export --help` for the full flag list.
