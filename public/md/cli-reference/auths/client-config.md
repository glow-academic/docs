# `glow auths client-config`

# `glow auths client-config`

Client Config Authentication records.

## Usage

```bash
glow auths client-config --id <id>
```

> Wire call: `POST /auth/client-config`. Run `glow auths client-config --help` for the full flag list.
