# `glow models search`

# `glow models search`

Search AI model configurations.

## Usage

```bash
glow models search
```

> Wire call: `POST /model/search`. Run `glow models search --help` for the full flag list.
