# `glow models export`

# `glow models export`

Export AI model configurations.

## Usage

```bash
glow models export --id <id>
```

> Wire call: `POST /model/export`. Run `glow models export --help` for the full flag list.
