# `glow models text_download`

# `glow models text_download`

Text Download AI model configurations.

## Usage

```bash
glow models text_download --id <id>
```

> Wire call: `POST /model/text_download`. Run `glow models text_download --help` for the full flag list.
