# `glow models problem`

# `glow models problem`

Problem AI model configurations.

## Usage

```bash
glow models problem --id <id>
```

> Wire call: `POST /model/problem`. Run `glow models problem --help` for the full flag list.
