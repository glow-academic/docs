# `glow models watch`

# `glow models watch`

Watch AI model configurations.

## Usage

```bash
glow models watch --id <id>
```

> Wire call: `POST /model/watch`. Run `glow models watch --help` for the full flag list.
