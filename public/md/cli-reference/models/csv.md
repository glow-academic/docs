# `glow models csv`

# `glow models csv`

Csv AI model configurations.

## Usage

```bash
glow models csv --id <id>
```

> Wire call: `POST /model/csv`. Run `glow models csv --help` for the full flag list.
