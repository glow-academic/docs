# `glow models generations`

# `glow models generations`

Generations AI model configurations.

## Usage

```bash
glow models generations --id <id>
```

> Wire call: `POST /model/generations`. Run `glow models generations --help` for the full flag list.
