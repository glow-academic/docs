# `glow models draft`

# `glow models draft`

Draft AI model configurations.

## Usage

```bash
glow models draft --id <id>
```

> Wire call: `POST /model/draft`. Run `glow models draft --help` for the full flag list.
