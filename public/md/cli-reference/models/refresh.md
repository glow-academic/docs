# `glow models refresh`

# `glow models refresh`

Refresh AI model configurations.

## Usage

```bash
glow models refresh --id <id>
```

> Wire call: `POST /model/refresh`. Run `glow models refresh --help` for the full flag list.
