# `glow parameters update`

# `glow parameters update`

Update Configuration parameters.

## Usage

```bash
glow parameters update --id <id>
```

> Wire call: `POST /parameter/update`. Run `glow parameters update --help` for the full flag list.
