# `glow parameters file_download`

# `glow parameters file_download`

File Download Configuration parameters.

## Usage

```bash
glow parameters file_download --id <id>
```

> Wire call: `POST /parameter/file_download`. Run `glow parameters file_download --help` for the full flag list.
