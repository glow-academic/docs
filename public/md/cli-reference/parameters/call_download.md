# `glow parameters call_download`

# `glow parameters call_download`

Call Download Configuration parameters.

## Usage

```bash
glow parameters call_download --id <id>
```

> Wire call: `POST /parameter/call_download`. Run `glow parameters call_download --help` for the full flag list.
