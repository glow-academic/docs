# `glow parameters drafts`

# `glow parameters drafts`

Drafts Configuration parameters.

## Usage

```bash
glow parameters drafts --id <id>
```

> Wire call: `POST /parameter/drafts`. Run `glow parameters drafts --help` for the full flag list.
