# `glow parameters generations`

# `glow parameters generations`

Generations Configuration parameters.

## Usage

```bash
glow parameters generations --id <id>
```

> Wire call: `POST /parameter/generations`. Run `glow parameters generations --help` for the full flag list.
