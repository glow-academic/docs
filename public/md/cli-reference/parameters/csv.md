# `glow parameters csv`

# `glow parameters csv`

Csv Configuration parameters.

## Usage

```bash
glow parameters csv --id <id>
```

> Wire call: `POST /parameter/csv`. Run `glow parameters csv --help` for the full flag list.
