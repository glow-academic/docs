# `glow parameters group`

# `glow parameters group`

Group Configuration parameters.

## Usage

```bash
glow parameters group --id <id>
```

> Wire call: `POST /parameter/group`. Run `glow parameters group --help` for the full flag list.
