# `glow parameters generate`

# `glow parameters generate`

Generate Configuration parameters.

## Usage

```bash
glow parameters generate --body '\{...\}'
```

> Wire call: `POST /parameter/generate`. Run `glow parameters generate --help` for the full flag list.
