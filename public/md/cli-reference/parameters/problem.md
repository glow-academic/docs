# `glow parameters problem`

# `glow parameters problem`

Problem Configuration parameters.

## Usage

```bash
glow parameters problem --id <id>
```

> Wire call: `POST /parameter/problem`. Run `glow parameters problem --help` for the full flag list.
