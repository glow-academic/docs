# `glow parameters title`

# `glow parameters title`

Title Configuration parameters.

## Usage

```bash
glow parameters title --id <id>
```

> Wire call: `POST /parameter/title`. Run `glow parameters title --help` for the full flag list.
