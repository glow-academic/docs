# `glow documents search`

# `glow documents search`

Search Document management.

## Usage

```bash
glow documents search
```

> Wire call: `POST /document/search`. Run `glow documents search --help` for the full flag list.
