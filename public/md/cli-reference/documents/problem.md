# `glow documents problem`

# `glow documents problem`

Problem Document management.

## Usage

```bash
glow documents problem --id <id>
```

> Wire call: `POST /document/problem`. Run `glow documents problem --help` for the full flag list.
