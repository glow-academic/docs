# `glow documents generate`

# `glow documents generate`

Generate Document management.

## Usage

```bash
glow documents generate --body '\{...\}'
```

> Wire call: `POST /document/generate`. Run `glow documents generate --help` for the full flag list.
