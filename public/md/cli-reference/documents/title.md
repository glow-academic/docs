# `glow documents title`

# `glow documents title`

Title Document management.

## Usage

```bash
glow documents title --id <id>
```

> Wire call: `POST /document/title`. Run `glow documents title --help` for the full flag list.
