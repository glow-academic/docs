# `glow documents file_preview`

# `glow documents file_preview`

File Preview Document management.

## Usage

```bash
glow documents file_preview --id <id>
```

> Wire call: `POST /document/file_preview`. Run `glow documents file_preview --help` for the full flag list.
