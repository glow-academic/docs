# `glow documents group`

# `glow documents group`

Group Document management.

## Usage

```bash
glow documents group --id <id>
```

> Wire call: `POST /document/group`. Run `glow documents group --help` for the full flag list.
