# `glow documents file_upload`

# `glow documents file_upload`

File Upload Document management.

## Usage

```bash
glow documents file_upload --id <id>
```

> Wire call: `POST /document/file_upload`. Run `glow documents file_upload --help` for the full flag list.
