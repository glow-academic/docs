# `glow documents refresh`

# `glow documents refresh`

Refresh Document management.

## Usage

```bash
glow documents refresh --id <id>
```

> Wire call: `POST /document/refresh`. Run `glow documents refresh --help` for the full flag list.
