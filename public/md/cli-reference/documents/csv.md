# `glow documents csv`

# `glow documents csv`

Csv Document management.

## Usage

```bash
glow documents csv --id <id>
```

> Wire call: `POST /document/csv`. Run `glow documents csv --help` for the full flag list.
