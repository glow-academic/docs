# `glow documents drafts`

# `glow documents drafts`

Drafts Document management.

## Usage

```bash
glow documents drafts --id <id>
```

> Wire call: `POST /document/drafts`. Run `glow documents drafts --help` for the full flag list.
