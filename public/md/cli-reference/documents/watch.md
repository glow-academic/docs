# `glow documents watch`

# `glow documents watch`

Watch Document management.

## Usage

```bash
glow documents watch --id <id>
```

> Wire call: `POST /document/watch`. Run `glow documents watch --help` for the full flag list.
