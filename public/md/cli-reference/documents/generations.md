# `glow documents generations`

# `glow documents generations`

Generations Document management.

## Usage

```bash
glow documents generations --id <id>
```

> Wire call: `POST /document/generations`. Run `glow documents generations --help` for the full flag list.
