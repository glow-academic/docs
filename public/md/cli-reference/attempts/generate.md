# `glow attempts generate`

# `glow attempts generate`

Generate Simulation attempts.

## Usage

```bash
glow attempts generate --body '\{...\}'
```

> Wire call: `POST /attempt/generate`. Run `glow attempts generate --help` for the full flag list.
