# `glow attempts dashboard get`

# `glow attempts dashboard get`

Get on dashboard (under attempts).

## Usage

```bash
glow attempts dashboard get --id <id>
```

> Wire call: `POST /attempt/dashboard`. Run `glow attempts dashboard get --help` for the full flag list.
