# `glow attempts chat_create`

# `glow attempts chat_create`

Chat Create Simulation attempts.

## Usage

```bash
glow attempts chat_create --id <id>
```

> Wire call: `POST /attempt/chat_create`. Run `glow attempts chat_create --help` for the full flag list.
