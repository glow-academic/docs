# `glow attempts context`

# `glow attempts context`

Context on attempts.

## Usage

```bash
glow attempts context --id <id>
```

> Wire call: `POST /attempt/context`. Run `glow attempts context --help` for the full flag list.
