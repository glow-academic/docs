# `glow attempts chat_strengths`

# `glow attempts chat_strengths`

Chat Strengths Simulation attempts.

## Usage

```bash
glow attempts chat_strengths --id <id>
```

> Wire call: `POST /attempt/chat_strengths`. Run `glow attempts chat_strengths --help` for the full flag list.
