# `glow attempts chat_audio`

# `glow attempts chat_audio`

Chat Audio Simulation attempts.

## Usage

```bash
glow attempts chat_audio --id <id>
```

> Wire call: `POST /attempt/chat_audio`. Run `glow attempts chat_audio --help` for the full flag list.
