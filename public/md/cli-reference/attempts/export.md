# `glow attempts export`

# `glow attempts export`

Export Simulation attempts.

## Usage

```bash
glow attempts export --id <id>
```

> Wire call: `POST /attempt/export`. Run `glow attempts export --help` for the full flag list.
