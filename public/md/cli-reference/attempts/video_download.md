# `glow attempts video_download`

# `glow attempts video_download`

Video Download Simulation attempts.

## Usage

```bash
glow attempts video_download --id <id>
```

> Wire call: `POST /attempt/video_download`. Run `glow attempts video_download --help` for the full flag list.
