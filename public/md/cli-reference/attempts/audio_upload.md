# `glow attempts audio_upload`

# `glow attempts audio_upload`

Audio Upload Simulation attempts.

## Usage

```bash
glow attempts audio_upload --id <id>
```

> Wire call: `POST /attempt/audio_upload`. Run `glow attempts audio_upload --help` for the full flag list.
