# `glow attempts home`

# `glow attempts home`

Home Simulation attempts.

## Usage

```bash
glow attempts home --id <id>
```

> Wire call: `POST /attempt/home`. Run `glow attempts home --help` for the full flag list.
