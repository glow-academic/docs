# `glow attempts problem`

# `glow attempts problem`

Problem on attempts.

## Usage

```bash
glow attempts problem --id <id>
```

> Wire call: `POST /attempt/problem`. Run `glow attempts problem --help` for the full flag list.
