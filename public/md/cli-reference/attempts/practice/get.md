# `glow attempts practice get`

# `glow attempts practice get`

Get on practice (under attempts).

## Usage

```bash
glow attempts practice get --id <id>
```

> Wire call: `POST /attempt/practice`. Run `glow attempts practice get --help` for the full flag list.
