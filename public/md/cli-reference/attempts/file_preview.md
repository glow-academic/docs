# `glow attempts file_preview`

# `glow attempts file_preview`

File Preview Simulation attempts.

## Usage

```bash
glow attempts file_preview --id <id>
```

> Wire call: `POST /attempt/file_preview`. Run `glow attempts file_preview --help` for the full flag list.
