# `glow attempts file_download`

# `glow attempts file_download`

File Download Simulation attempts.

## Usage

```bash
glow attempts file_download --id <id>
```

> Wire call: `POST /attempt/file_download`. Run `glow attempts file_download --help` for the full flag list.
