# `glow attempts stop`

# `glow attempts stop`

Stop Simulation attempts.

## Usage

```bash
glow attempts stop --id <id>
```

> Wire call: `POST /attempt/stop`. Run `glow attempts stop --help` for the full flag list.
