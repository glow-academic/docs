# `glow attempts call_download`

# `glow attempts call_download`

Call Download Simulation attempts.

## Usage

```bash
glow attempts call_download --id <id>
```

> Wire call: `POST /attempt/call_download`. Run `glow attempts call_download --help` for the full flag list.
