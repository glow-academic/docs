# `glow attempts draft`

# `glow attempts draft`

Draft on attempts.

## Usage

```bash
glow attempts draft --id <id>
```

> Wire call: `POST /attempt/draft`. Run `glow attempts draft --help` for the full flag list.
