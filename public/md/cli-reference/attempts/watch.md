# `glow attempts watch`

# `glow attempts watch`

Watch Simulation attempts.

## Usage

```bash
glow attempts watch --id <id>
```

> Wire call: `POST /attempt/watch`. Run `glow attempts watch --help` for the full flag list.
