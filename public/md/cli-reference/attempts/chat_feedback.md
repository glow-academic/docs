# `glow attempts chat_feedback`

# `glow attempts chat_feedback`

Chat Feedback Simulation attempts.

## Usage

```bash
glow attempts chat_feedback --id <id>
```

> Wire call: `POST /attempt/chat_feedback`. Run `glow attempts chat_feedback --help` for the full flag list.
