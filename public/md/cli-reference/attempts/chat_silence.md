# `glow attempts chat_silence`

# `glow attempts chat_silence`

Chat Silence Simulation attempts.

## Usage

```bash
glow attempts chat_silence --id <id>
```

> Wire call: `POST /attempt/chat_silence`. Run `glow attempts chat_silence --help` for the full flag list.
