# `glow attempts chat message`

# `glow attempts chat message`

Message on chat (under attempts).

## Usage

```bash
glow attempts chat message --id <id>
```

> Wire call: `POST /attempt/chat_message`. Run `glow attempts chat message --help` for the full flag list.
