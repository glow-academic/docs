# `glow attempts chat create`

# `glow attempts chat create`

Create on chat (under attempts).

## Usage

```bash
glow attempts chat create --body '\{...\}'
```

> Wire call: `POST /attempt/chat_create`. Run `glow attempts chat create --help` for the full flag list.
