# `glow attempts chat hints`

# `glow attempts chat hints`

Hints on chat (under attempts).

## Usage

```bash
glow attempts chat hints --id <id>
```

> Wire call: `POST /attempt/chat_hints`. Run `glow attempts chat hints --help` for the full flag list.
