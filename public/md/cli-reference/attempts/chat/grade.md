# `glow attempts chat grade`

# `glow attempts chat grade`

Grade on chat (under attempts).

## Usage

```bash
glow attempts chat grade --id <id>
```

> Wire call: `POST /attempt/chat_grade`. Run `glow attempts chat grade --help` for the full flag list.
