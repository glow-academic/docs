# `glow attempts chat silence`

# `glow attempts chat silence`

Silence on chat (under attempts).

## Usage

```bash
glow attempts chat silence --id <id>
```

> Wire call: `POST /attempt/chat_silence`. Run `glow attempts chat silence --help` for the full flag list.
