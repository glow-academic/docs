# `glow attempts chat feedback`

# `glow attempts chat feedback`

Feedback on chat (under attempts).

## Usage

```bash
glow attempts chat feedback --id <id>
```

> Wire call: `POST /attempt/chat_feedback`. Run `glow attempts chat feedback --help` for the full flag list.
