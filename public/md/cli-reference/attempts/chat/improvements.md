# `glow attempts chat improvements`

# `glow attempts chat improvements`

Improvements on chat (under attempts).

## Usage

```bash
glow attempts chat improvements --id <id>
```

> Wire call: `POST /attempt/chat_improvements`. Run `glow attempts chat improvements --help` for the full flag list.
