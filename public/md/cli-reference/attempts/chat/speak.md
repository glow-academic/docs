# `glow attempts chat speak`

# `glow attempts chat speak`

Speak on chat (under attempts).

## Usage

```bash
glow attempts chat speak --id <id>
```

> Wire call: `POST /attempt/chat_speak`. Run `glow attempts chat speak --help` for the full flag list.
