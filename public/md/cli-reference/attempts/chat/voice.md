# `glow attempts chat voice`

# `glow attempts chat voice`

Voice on chat (under attempts).

## Usage

```bash
glow attempts chat voice --id <id>
```

> Wire call: `POST /attempt/chat_voice`. Run `glow attempts chat voice --help` for the full flag list.
