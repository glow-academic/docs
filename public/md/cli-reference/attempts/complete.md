# `glow attempts complete`

# `glow attempts complete`

Complete Simulation attempts.

## Usage

```bash
glow attempts complete --id <id>
```

> Wire call: `POST /attempt/complete`. Run `glow attempts complete --help` for the full flag list.
