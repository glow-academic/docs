# `glow attempts group`

# `glow attempts group`

Group Simulation attempts.

## Usage

```bash
glow attempts group --id <id>
```

> Wire call: `POST /attempt/group`. Run `glow attempts group --help` for the full flag list.
