# `glow attempts home get`

# `glow attempts home get`

Get on home (under attempts).

## Usage

```bash
glow attempts home get --id <id>
```

> Wire call: `POST /attempt/home`. Run `glow attempts home get --help` for the full flag list.
