# `glow attempts chat_speak`

# `glow attempts chat_speak`

Chat Speak Simulation attempts.

## Usage

```bash
glow attempts chat_speak --id <id>
```

> Wire call: `POST /attempt/chat_speak`. Run `glow attempts chat_speak --help` for the full flag list.
