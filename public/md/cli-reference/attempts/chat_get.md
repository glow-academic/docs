# `glow attempts chat_get`

# `glow attempts chat_get`

Chat Get Simulation attempts.

## Usage

```bash
glow attempts chat_get --id <id>
```

> Wire call: `POST /attempt/chat_get`. Run `glow attempts chat_get --help` for the full flag list.
