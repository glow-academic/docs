# `glow attempts leaderboard`

# `glow attempts leaderboard`

Leaderboard Simulation attempts.

## Usage

```bash
glow attempts leaderboard --id <id>
```

> Wire call: `POST /attempt/leaderboard`. Run `glow attempts leaderboard --help` for the full flag list.
