# `glow attempts drafts`

# `glow attempts drafts`

Drafts Simulation attempts.

## Usage

```bash
glow attempts drafts --id <id>
```

> Wire call: `POST /attempt/drafts`. Run `glow attempts drafts --help` for the full flag list.
