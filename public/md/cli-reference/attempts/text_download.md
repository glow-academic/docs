# `glow attempts text_download`

# `glow attempts text_download`

Text Download Simulation attempts.

## Usage

```bash
glow attempts text_download --id <id>
```

> Wire call: `POST /attempt/text_download`. Run `glow attempts text_download --help` for the full flag list.
