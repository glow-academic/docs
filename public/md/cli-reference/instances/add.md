# `glow instances add`

Add a new instance

## Usage

```bash
glow instances add name <name> --url <url>
```

## Options

| Flag | Required | Description |
|---|---|---|
| `name` | Yes | Instance name (e.g. "prod", "staging") |
| `--url` | Yes | Instance URL |