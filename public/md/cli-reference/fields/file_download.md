# `glow fields file_download`

# `glow fields file_download`

File Download Custom fields.

## Usage

```bash
glow fields file_download --id <id>
```

> Wire call: `POST /field/file_download`. Run `glow fields file_download --help` for the full flag list.
