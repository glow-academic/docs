# `glow fields call_download`

# `glow fields call_download`

Call Download Custom fields.

## Usage

```bash
glow fields call_download --id <id>
```

> Wire call: `POST /field/call_download`. Run `glow fields call_download --help` for the full flag list.
