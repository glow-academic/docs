# `glow fields watch`

# `glow fields watch`

Watch Custom fields.

## Usage

```bash
glow fields watch --id <id>
```

> Wire call: `POST /field/watch`. Run `glow fields watch --help` for the full flag list.
