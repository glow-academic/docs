# `glow fields export`

# `glow fields export`

Export Custom fields.

## Usage

```bash
glow fields export --id <id>
```

> Wire call: `POST /field/export`. Run `glow fields export --help` for the full flag list.
