# `glow fields generate`

# `glow fields generate`

Generate Custom fields.

## Usage

```bash
glow fields generate --body '\{...\}'
```

> Wire call: `POST /field/generate`. Run `glow fields generate --help` for the full flag list.
