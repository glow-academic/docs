# `glow evals duplicate`

# `glow evals duplicate`

Duplicate Evaluation configurations.

## Usage

```bash
glow evals duplicate --id <id>
```

> Wire call: `POST /eval/duplicate`. Run `glow evals duplicate --help` for the full flag list.
