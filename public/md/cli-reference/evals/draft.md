# `glow evals draft`

# `glow evals draft`

Draft Evaluation configurations.

## Usage

```bash
glow evals draft --id <id>
```

> Wire call: `POST /eval/draft`. Run `glow evals draft --help` for the full flag list.
