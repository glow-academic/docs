# `glow evals text_download`

# `glow evals text_download`

Text Download Evaluation configurations.

## Usage

```bash
glow evals text_download --id <id>
```

> Wire call: `POST /eval/text_download`. Run `glow evals text_download --help` for the full flag list.
