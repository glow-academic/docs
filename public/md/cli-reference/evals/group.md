# `glow evals group`

# `glow evals group`

Group Evaluation configurations.

## Usage

```bash
glow evals group --id <id>
```

> Wire call: `POST /eval/group`. Run `glow evals group --help` for the full flag list.
