# `glow evals drafts`

# `glow evals drafts`

Drafts Evaluation configurations.

## Usage

```bash
glow evals drafts --id <id>
```

> Wire call: `POST /eval/drafts`. Run `glow evals drafts --help` for the full flag list.
