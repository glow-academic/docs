# `glow evals refresh`

# `glow evals refresh`

Refresh Evaluation configurations.

## Usage

```bash
glow evals refresh --id <id>
```

> Wire call: `POST /eval/refresh`. Run `glow evals refresh --help` for the full flag list.
