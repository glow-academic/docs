# `glow evals export`

# `glow evals export`

Export Evaluation configurations.

## Usage

```bash
glow evals export --id <id>
```

> Wire call: `POST /eval/export`. Run `glow evals export --help` for the full flag list.
