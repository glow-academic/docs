# `glow evals problem`

# `glow evals problem`

Problem Evaluation configurations.

## Usage

```bash
glow evals problem --id <id>
```

> Wire call: `POST /eval/problem`. Run `glow evals problem --help` for the full flag list.
