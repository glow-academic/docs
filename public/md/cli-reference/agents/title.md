# `glow agents title`

# `glow agents title`

Title AI agents.

## Usage

```bash
glow agents title --id <id>
```

> Wire call: `POST /agent/title`. Run `glow agents title --help` for the full flag list.
