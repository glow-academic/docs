# `glow agents refresh`

# `glow agents refresh`

Refresh AI agents.

## Usage

```bash
glow agents refresh --id <id>
```

> Wire call: `POST /agent/refresh`. Run `glow agents refresh --help` for the full flag list.
