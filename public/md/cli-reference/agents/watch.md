# `glow agents watch`

# `glow agents watch`

Watch AI agents.

## Usage

```bash
glow agents watch --id <id>
```

> Wire call: `POST /agent/watch`. Run `glow agents watch --help` for the full flag list.
