# `glow agents file_download`

# `glow agents file_download`

File Download AI agents.

## Usage

```bash
glow agents file_download --id <id>
```

> Wire call: `POST /agent/file_download`. Run `glow agents file_download --help` for the full flag list.
