# `glow agents export`

# `glow agents export`

Export AI agents.

## Usage

```bash
glow agents export --id <id>
```

> Wire call: `POST /agent/export`. Run `glow agents export --help` for the full flag list.
