# `glow agents generations`

# `glow agents generations`

Generations AI agents.

## Usage

```bash
glow agents generations --id <id>
```

> Wire call: `POST /agent/generations`. Run `glow agents generations --help` for the full flag list.
