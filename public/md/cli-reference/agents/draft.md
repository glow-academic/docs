# `glow agents draft`

# `glow agents draft`

Draft AI agents.

## Usage

```bash
glow agents draft --id <id>
```

> Wire call: `POST /agent/draft`. Run `glow agents draft --help` for the full flag list.
