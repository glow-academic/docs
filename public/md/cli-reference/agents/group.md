# `glow agents group`

# `glow agents group`

Group AI agents.

## Usage

```bash
glow agents group --id <id>
```

> Wire call: `POST /agent/group`. Run `glow agents group --help` for the full flag list.
