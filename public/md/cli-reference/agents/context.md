# `glow agents context`

# `glow agents context`

Context AI agents.

## Usage

```bash
glow agents context --id <id>
```

> Wire call: `POST /agent/context`. Run `glow agents context --help` for the full flag list.
