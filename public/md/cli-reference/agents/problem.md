# `glow agents problem`

# `glow agents problem`

Problem AI agents.

## Usage

```bash
glow agents problem --id <id>
```

> Wire call: `POST /agent/problem`. Run `glow agents problem --help` for the full flag list.
