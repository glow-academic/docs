# `glow scenarios image_upload`

# `glow scenarios image_upload`

Image Upload Simulation scenarios.

## Usage

```bash
glow scenarios image_upload --id <id>
```

> Wire call: `POST /scenario/image_upload`. Run `glow scenarios image_upload --help` for the full flag list.
