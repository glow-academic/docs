# `glow scenarios video_upload`

# `glow scenarios video_upload`

Video Upload Simulation scenarios.

## Usage

```bash
glow scenarios video_upload --id <id>
```

> Wire call: `POST /scenario/video_upload`. Run `glow scenarios video_upload --help` for the full flag list.
