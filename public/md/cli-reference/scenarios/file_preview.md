# `glow scenarios file_preview`

# `glow scenarios file_preview`

File Preview Simulation scenarios.

## Usage

```bash
glow scenarios file_preview --id <id>
```

> Wire call: `POST /scenario/file_preview`. Run `glow scenarios file_preview --help` for the full flag list.
