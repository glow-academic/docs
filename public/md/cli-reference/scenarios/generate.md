# `glow scenarios generate`

# `glow scenarios generate`

Generate Simulation scenarios.

## Usage

```bash
glow scenarios generate --body '\{...\}'
```

> Wire call: `POST /scenario/generate`. Run `glow scenarios generate --help` for the full flag list.
