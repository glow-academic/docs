# `glow scenarios call_download`

# `glow scenarios call_download`

Call Download Simulation scenarios.

## Usage

```bash
glow scenarios call_download --id <id>
```

> Wire call: `POST /scenario/call_download`. Run `glow scenarios call_download --help` for the full flag list.
