# `glow scenarios text_download`

# `glow scenarios text_download`

Text Download Simulation scenarios.

## Usage

```bash
glow scenarios text_download --id <id>
```

> Wire call: `POST /scenario/text_download`. Run `glow scenarios text_download --help` for the full flag list.
