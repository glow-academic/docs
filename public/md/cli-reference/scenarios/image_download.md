# `glow scenarios image_download`

# `glow scenarios image_download`

Image Download Simulation scenarios.

## Usage

```bash
glow scenarios image_download --id <id>
```

> Wire call: `POST /scenario/image_download`. Run `glow scenarios image_download --help` for the full flag list.
