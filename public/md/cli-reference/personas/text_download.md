# `glow personas text_download`

# `glow personas text_download`

Text Download AI personas for simulation.

## Usage

```bash
glow personas text_download --id <id>
```

> Wire call: `POST /persona/text_download`. Run `glow personas text_download --help` for the full flag list.
