# `glow personas group`

# `glow personas group`

Group AI personas for simulation.

## Usage

```bash
glow personas group --id <id>
```

> Wire call: `POST /persona/group`. Run `glow personas group --help` for the full flag list.
