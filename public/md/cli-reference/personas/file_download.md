# `glow personas file_download`

# `glow personas file_download`

File Download AI personas for simulation.

## Usage

```bash
glow personas file_download --id <id>
```

> Wire call: `POST /persona/file_download`. Run `glow personas file_download --help` for the full flag list.
