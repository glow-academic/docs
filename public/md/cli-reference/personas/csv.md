# `glow personas csv`

# `glow personas csv`

Csv AI personas for simulation.

## Usage

```bash
glow personas csv --id <id>
```

> Wire call: `POST /persona/csv`. Run `glow personas csv --help` for the full flag list.
