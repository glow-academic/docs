# `glow personas context`

# `glow personas context`

Context AI personas for simulation.

## Usage

```bash
glow personas context --id <id>
```

> Wire call: `POST /persona/context`. Run `glow personas context --help` for the full flag list.
