# `glow personas drafts`

# `glow personas drafts`

Drafts AI personas for simulation.

## Usage

```bash
glow personas drafts --id <id>
```

> Wire call: `POST /persona/drafts`. Run `glow personas drafts --help` for the full flag list.
