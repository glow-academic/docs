# `glow personas draft`

# `glow personas draft`

Draft AI personas for simulation.

## Usage

```bash
glow personas draft --id <id>
```

> Wire call: `POST /persona/draft`. Run `glow personas draft --help` for the full flag list.
