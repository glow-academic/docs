# `glow personas export`

# `glow personas export`

Export AI personas for simulation.

## Usage

```bash
glow personas export --id <id>
```

> Wire call: `POST /persona/export`. Run `glow personas export --help` for the full flag list.
