# `glow personas update`

# `glow personas update`

Update AI personas for simulation.

## Usage

```bash
glow personas update --id <id>
```

> Wire call: `POST /persona/update`. Run `glow personas update --help` for the full flag list.
