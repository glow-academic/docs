# `glow personas title`

# `glow personas title`

Title AI personas for simulation.

## Usage

```bash
glow personas title --id <id>
```

> Wire call: `POST /persona/title`. Run `glow personas title --help` for the full flag list.
