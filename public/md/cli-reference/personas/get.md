# `glow personas get`

# `glow personas get`

Get AI personas for simulation.

## Usage

```bash
glow personas get --id <id>
```

> Wire call: `POST /persona/get`. Run `glow personas get --help` for the full flag list.
