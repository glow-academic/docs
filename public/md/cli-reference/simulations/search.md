# `glow simulations search`

# `glow simulations search`

Search Simulation configurations.

## Usage

```bash
glow simulations search
```

> Wire call: `POST /simulation/search`. Run `glow simulations search --help` for the full flag list.
