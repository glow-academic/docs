# `glow simulations export`

# `glow simulations export`

Export Simulation configurations.

## Usage

```bash
glow simulations export --id <id>
```

> Wire call: `POST /simulation/export`. Run `glow simulations export --help` for the full flag list.
