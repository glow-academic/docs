# `glow simulations create`

# `glow simulations create`

Create Simulation configurations.

## Usage

```bash
glow simulations create --body '\{...\}'
```

> Wire call: `POST /simulation/create`. Run `glow simulations create --help` for the full flag list.
