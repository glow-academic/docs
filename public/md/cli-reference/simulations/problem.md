# `glow simulations problem`

# `glow simulations problem`

Problem Simulation configurations.

## Usage

```bash
glow simulations problem --id <id>
```

> Wire call: `POST /simulation/problem`. Run `glow simulations problem --help` for the full flag list.
