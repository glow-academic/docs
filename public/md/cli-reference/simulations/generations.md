# `glow simulations generations`

# `glow simulations generations`

Generations Simulation configurations.

## Usage

```bash
glow simulations generations --id <id>
```

> Wire call: `POST /simulation/generations`. Run `glow simulations generations --help` for the full flag list.
