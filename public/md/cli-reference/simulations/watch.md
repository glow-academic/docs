# `glow simulations watch`

# `glow simulations watch`

Watch Simulation configurations.

## Usage

```bash
glow simulations watch --id <id>
```

> Wire call: `POST /simulation/watch`. Run `glow simulations watch --help` for the full flag list.
