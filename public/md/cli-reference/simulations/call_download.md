# `glow simulations call_download`

# `glow simulations call_download`

Call Download Simulation configurations.

## Usage

```bash
glow simulations call_download --id <id>
```

> Wire call: `POST /simulation/call_download`. Run `glow simulations call_download --help` for the full flag list.
