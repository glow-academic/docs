# `glow simulations group`

# `glow simulations group`

Group Simulation configurations.

## Usage

```bash
glow simulations group --id <id>
```

> Wire call: `POST /simulation/group`. Run `glow simulations group --help` for the full flag list.
