# `glow simulations text_download`

# `glow simulations text_download`

Text Download Simulation configurations.

## Usage

```bash
glow simulations text_download --id <id>
```

> Wire call: `POST /simulation/text_download`. Run `glow simulations text_download --help` for the full flag list.
