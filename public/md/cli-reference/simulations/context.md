# `glow simulations context`

# `glow simulations context`

Context Simulation configurations.

## Usage

```bash
glow simulations context --id <id>
```

> Wire call: `POST /simulation/context`. Run `glow simulations context --help` for the full flag list.
