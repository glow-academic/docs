# `glow system text_download`

# `glow system text_download`

Text Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system text_download --id <id>
```

> Wire call: `POST /system/text_download`. Run `glow system text_download --help` for the full flag list.
