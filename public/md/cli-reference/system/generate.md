# `glow system generate`

# `glow system generate`

Generate System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system generate --body '\{...\}'
```

> Wire call: `POST /system/generate`. Run `glow system generate --help` for the full flag list.
