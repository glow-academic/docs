# `glow system audio_download`

# `glow system audio_download`

Audio Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system audio_download --id <id>
```

> Wire call: `POST /system/audio_download`. Run `glow system audio_download --help` for the full flag list.
