# `glow system call_download`

# `glow system call_download`

Call Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system call_download --id <id>
```

> Wire call: `POST /system/call_download`. Run `glow system call_download --help` for the full flag list.
