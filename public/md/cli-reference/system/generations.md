# `glow system generations`

# `glow system generations`

Generations System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system generations --id <id>
```

> Wire call: `POST /system/generations`. Run `glow system generations --help` for the full flag list.
