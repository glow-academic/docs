# `glow system health get`

# `glow system health get`

Get on health (under system).

## Usage

```bash
glow system health get --id <id>
```

> Wire call: `POST /system/health`. Run `glow system health get --help` for the full flag list.
