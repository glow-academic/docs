# `glow system image_download`

# `glow system image_download`

Image Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system image_download --id <id>
```

> Wire call: `POST /system/image_download`. Run `glow system image_download --help` for the full flag list.
