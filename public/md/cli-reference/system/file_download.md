# `glow system file_download`

# `glow system file_download`

File Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system file_download --id <id>
```

> Wire call: `POST /system/file_download`. Run `glow system file_download --help` for the full flag list.
