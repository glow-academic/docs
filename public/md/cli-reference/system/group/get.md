# `glow system group get`

# `glow system group get`

Get on group (under system).

## Usage

```bash
glow system group get --id <id>
```

> Wire call: `POST /system/group`. Run `glow system group get --help` for the full flag list.
