# `glow system export`

# `glow system export`

Export System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system export --id <id>
```

> Wire call: `POST /system/export`. Run `glow system export --help` for the full flag list.
