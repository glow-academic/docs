# `glow system refresh`

# `glow system refresh`

Refresh on system.

## Usage

```bash
glow system refresh --id <id>
```

> Wire call: `POST /system/refresh`. Run `glow system refresh --help` for the full flag list.
