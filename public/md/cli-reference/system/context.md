# `glow system context`

# `glow system context`

Context System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system context --id <id>
```

> Wire call: `POST /system/context`. Run `glow system context --help` for the full flag list.
