# `glow system pricing get`

# `glow system pricing get`

Get on pricing (under system).

## Usage

```bash
glow system pricing get --id <id>
```

> Wire call: `POST /system/pricing`. Run `glow system pricing get --help` for the full flag list.
