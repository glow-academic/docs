# `glow system watch`

# `glow system watch`

Watch on system.

## Usage

```bash
glow system watch --id <id>
```

> Wire call: `POST /system/watch`. Run `glow system watch --help` for the full flag list.
