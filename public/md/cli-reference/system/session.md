# `glow system session`

# `glow system session`

Session System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system session --id <id>
```

> Wire call: `POST /system/session`. Run `glow system session --help` for the full flag list.
