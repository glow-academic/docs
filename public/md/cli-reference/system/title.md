# `glow system title`

# `glow system title`

Title System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system title --id <id>
```

> Wire call: `POST /system/title`. Run `glow system title --help` for the full flag list.
