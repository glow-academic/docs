# `glow system problem`

# `glow system problem`

Problem on system.

## Usage

```bash
glow system problem --id <id>
```

> Wire call: `POST /system/problem`. Run `glow system problem --help` for the full flag list.
