# `glow system video_download`

# `glow system video_download`

Video Download System views + cross-artifact ops (activity, health, sessions, groups, pricing, downloads, ...).

## Usage

```bash
glow system video_download --id <id>
```

> Wire call: `POST /system/video_download`. Run `glow system video_download --help` for the full flag list.
