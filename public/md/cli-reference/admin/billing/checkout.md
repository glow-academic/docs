# `glow admin billing checkout`

Start checkout for a plan

## Usage

```bash
glow admin billing checkout org_id <org_id> [--plan]
```

## Options

| Flag | Required | Description |
|---|---|---|
| `org_id` | Yes | Organization ID |
| `--plan` | No | Plan ID (e.g. "pro") |