# `glow tests invocation_get`

# `glow tests invocation_get`

Invocation Get Test sessions.

## Usage

```bash
glow tests invocation_get --id <id>
```

> Wire call: `POST /test/invocation_get`. Run `glow tests invocation_get --help` for the full flag list.
