# `glow tests export`

# `glow tests export`

Export on tests.

## Usage

```bash
glow tests export --id <id>
```

> Wire call: `POST /test/export`. Run `glow tests export --help` for the full flag list.
