# `glow tests context`

# `glow tests context`

Context on tests.

## Usage

```bash
glow tests context --id <id>
```

> Wire call: `POST /test/context`. Run `glow tests context --help` for the full flag list.
