# `glow tests invocation_run`

# `glow tests invocation_run`

Invocation Run Test sessions.

## Usage

```bash
glow tests invocation_run --id <id>
```

> Wire call: `POST /test/invocation_run`. Run `glow tests invocation_run --help` for the full flag list.
