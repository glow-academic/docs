# `glow tests drafts`

# `glow tests drafts`

Drafts Test sessions.

## Usage

```bash
glow tests drafts --id <id>
```

> Wire call: `POST /test/drafts`. Run `glow tests drafts --help` for the full flag list.
