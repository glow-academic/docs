# `glow tests invocation terminate`

# `glow tests invocation terminate`

Terminate on invocation (under tests).

## Usage

```bash
glow tests invocation terminate --id <id>
```

> Wire call: `POST /test/invocation_terminate`. Run `glow tests invocation terminate --help` for the full flag list.
