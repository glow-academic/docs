# `glow tests invocation complete`

# `glow tests invocation complete`

Complete on invocation (under tests).

## Usage

```bash
glow tests invocation complete --id <id>
```

> Wire call: `POST /test/invocation_complete`. Run `glow tests invocation complete --help` for the full flag list.
