# `glow tests invocation run`

# `glow tests invocation run`

Run on invocation (under tests).

## Usage

```bash
glow tests invocation run --id <id>
```

> Wire call: `POST /test/invocation_run`. Run `glow tests invocation run --help` for the full flag list.
