# `glow tests invocation get`

# `glow tests invocation get`

Get on invocation (under tests).

## Usage

```bash
glow tests invocation get --id <id>
```

> Wire call: `POST /test/invocation_get`. Run `glow tests invocation get --help` for the full flag list.
