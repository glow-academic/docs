# `glow tests decrypt`

# `glow tests decrypt`

Decrypt Test sessions.

## Usage

```bash
glow tests decrypt --id <id>
```

> Wire call: `POST /test/decrypt`. Run `glow tests decrypt --help` for the full flag list.
