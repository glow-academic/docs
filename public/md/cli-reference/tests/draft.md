# `glow tests draft`

# `glow tests draft`

Draft Test sessions.

## Usage

```bash
glow tests draft --id <id>
```

> Wire call: `POST /test/draft`. Run `glow tests draft --help` for the full flag list.
