# `glow tests title`

# `glow tests title`

Title Test sessions.

## Usage

```bash
glow tests title --id <id>
```

> Wire call: `POST /test/title`. Run `glow tests title --help` for the full flag list.
