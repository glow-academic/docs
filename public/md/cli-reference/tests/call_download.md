# `glow tests call_download`

# `glow tests call_download`

Call Download Test sessions.

## Usage

```bash
glow tests call_download --id <id>
```

> Wire call: `POST /test/call_download`. Run `glow tests call_download --help` for the full flag list.
