# `glow tests file_download`

# `glow tests file_download`

File Download Test sessions.

## Usage

```bash
glow tests file_download --id <id>
```

> Wire call: `POST /test/file_download`. Run `glow tests file_download --help` for the full flag list.
