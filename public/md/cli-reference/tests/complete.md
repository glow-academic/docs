# `glow tests complete`

# `glow tests complete`

Complete Test sessions.

## Usage

```bash
glow tests complete --id <id>
```

> Wire call: `POST /test/complete`. Run `glow tests complete --help` for the full flag list.
