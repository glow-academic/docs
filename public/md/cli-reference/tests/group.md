# `glow tests group`

# `glow tests group`

Group Test sessions.

## Usage

```bash
glow tests group --id <id>
```

> Wire call: `POST /test/group`. Run `glow tests group --help` for the full flag list.
