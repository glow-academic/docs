# `glow tests refresh`

# `glow tests refresh`

Refresh on tests.

## Usage

```bash
glow tests refresh --id <id>
```

> Wire call: `POST /test/refresh`. Run `glow tests refresh --help` for the full flag list.
