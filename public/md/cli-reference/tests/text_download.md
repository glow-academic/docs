# `glow tests text_download`

# `glow tests text_download`

Text Download Test sessions.

## Usage

```bash
glow tests text_download --id <id>
```

> Wire call: `POST /test/text_download`. Run `glow tests text_download --help` for the full flag list.
