# `glow profiles problem`

# `glow profiles problem`

Problem User profiles.

## Usage

```bash
glow profiles problem --id <id>
```

> Wire call: `POST /profile/problem`. Run `glow profiles problem --help` for the full flag list.
