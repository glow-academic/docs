# `glow profiles generate`

# `glow profiles generate`

Generate User profiles.

## Usage

```bash
glow profiles generate --body '\{...\}'
```

> Wire call: `POST /profile/generate`. Run `glow profiles generate --help` for the full flag list.
