# `glow profiles watch`

# `glow profiles watch`

Watch User profiles.

## Usage

```bash
glow profiles watch --id <id>
```

> Wire call: `POST /profile/watch`. Run `glow profiles watch --help` for the full flag list.
