# `glow profiles emulate`

# `glow profiles emulate`

Emulate User profiles.

## Usage

```bash
glow profiles emulate --id <id>
```

> Wire call: `POST /profile/emulate`. Run `glow profiles emulate --help` for the full flag list.
