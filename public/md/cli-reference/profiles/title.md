# `glow profiles title`

# `glow profiles title`

Title User profiles.

## Usage

```bash
glow profiles title --id <id>
```

> Wire call: `POST /profile/title`. Run `glow profiles title --help` for the full flag list.
