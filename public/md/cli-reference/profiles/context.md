# `glow profiles context`

# `glow profiles context`

Context User profiles.

## Usage

```bash
glow profiles context --id <id>
```

> Wire call: `POST /profile/context`. Run `glow profiles context --help` for the full flag list.
