# `glow profiles delete`

# `glow profiles delete`

Delete User profiles.

## Usage

```bash
glow profiles delete --id <id>
```

> Wire call: `POST /profile/delete`. Run `glow profiles delete --help` for the full flag list.
