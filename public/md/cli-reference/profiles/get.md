# `glow profiles get`

# `glow profiles get`

Get User profiles.

## Usage

```bash
glow profiles get --id <id>
```

> Wire call: `POST /profile/get`. Run `glow profiles get --help` for the full flag list.
