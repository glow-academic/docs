# `glow profiles file_download`

# `glow profiles file_download`

File Download User profiles.

## Usage

```bash
glow profiles file_download --id <id>
```

> Wire call: `POST /profile/file_download`. Run `glow profiles file_download --help` for the full flag list.
