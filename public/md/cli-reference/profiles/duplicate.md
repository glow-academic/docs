# `glow profiles duplicate`

# `glow profiles duplicate`

Duplicate User profiles.

## Usage

```bash
glow profiles duplicate --id <id>
```

> Wire call: `POST /profile/duplicate`. Run `glow profiles duplicate --help` for the full flag list.
