# `glow profiles export`

# `glow profiles export`

Export User profiles.

## Usage

```bash
glow profiles export --id <id>
```

> Wire call: `POST /profile/export`. Run `glow profiles export --help` for the full flag list.
