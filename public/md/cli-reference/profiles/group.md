# `glow profiles group`

# `glow profiles group`

Group User profiles.

## Usage

```bash
glow profiles group --id <id>
```

> Wire call: `POST /profile/group`. Run `glow profiles group --help` for the full flag list.
