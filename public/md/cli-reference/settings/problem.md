# `glow settings problem`

# `glow settings problem`

Problem Instance settings.

## Usage

```bash
glow settings problem --id <id>
```

> Wire call: `POST /setting/problem`. Run `glow settings problem --help` for the full flag list.
