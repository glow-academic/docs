# `glow settings text_download`

# `glow settings text_download`

Text Download Instance settings.

## Usage

```bash
glow settings text_download --id <id>
```

> Wire call: `POST /setting/text_download`. Run `glow settings text_download --help` for the full flag list.
