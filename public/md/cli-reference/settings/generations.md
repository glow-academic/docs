# `glow settings generations`

# `glow settings generations`

Generations Instance settings.

## Usage

```bash
glow settings generations --id <id>
```

> Wire call: `POST /setting/generations`. Run `glow settings generations --help` for the full flag list.
