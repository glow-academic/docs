# `glow settings draft`

# `glow settings draft`

Draft Instance settings.

## Usage

```bash
glow settings draft --id <id>
```

> Wire call: `POST /setting/draft`. Run `glow settings draft --help` for the full flag list.
