# `glow settings generate`

# `glow settings generate`

Generate Instance settings.

## Usage

```bash
glow settings generate --body '\{...\}'
```

> Wire call: `POST /setting/generate`. Run `glow settings generate --help` for the full flag list.
