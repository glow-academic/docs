# `glow settings context`

# `glow settings context`

Context Instance settings.

## Usage

```bash
glow settings context --id <id>
```

> Wire call: `POST /setting/context`. Run `glow settings context --help` for the full flag list.
