# `glow rubrics search`

# `glow rubrics search`

Search Grading rubrics.

## Usage

```bash
glow rubrics search
```

> Wire call: `POST /rubric/search`. Run `glow rubrics search --help` for the full flag list.
