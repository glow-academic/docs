# `glow rubrics export`

# `glow rubrics export`

Export Grading rubrics.

## Usage

```bash
glow rubrics export --id <id>
```

> Wire call: `POST /rubric/export`. Run `glow rubrics export --help` for the full flag list.
