# `glow rubrics refresh`

# `glow rubrics refresh`

Refresh Grading rubrics.

## Usage

```bash
glow rubrics refresh --id <id>
```

> Wire call: `POST /rubric/refresh`. Run `glow rubrics refresh --help` for the full flag list.
