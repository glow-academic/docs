# `glow rubrics problem`

# `glow rubrics problem`

Problem Grading rubrics.

## Usage

```bash
glow rubrics problem --id <id>
```

> Wire call: `POST /rubric/problem`. Run `glow rubrics problem --help` for the full flag list.
