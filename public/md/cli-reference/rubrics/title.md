# `glow rubrics title`

# `glow rubrics title`

Title Grading rubrics.

## Usage

```bash
glow rubrics title --id <id>
```

> Wire call: `POST /rubric/title`. Run `glow rubrics title --help` for the full flag list.
