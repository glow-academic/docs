# `glow rubrics csv`

# `glow rubrics csv`

Csv Grading rubrics.

## Usage

```bash
glow rubrics csv --id <id>
```

> Wire call: `POST /rubric/csv`. Run `glow rubrics csv --help` for the full flag list.
