# `glow rubrics group`

# `glow rubrics group`

Group Grading rubrics.

## Usage

```bash
glow rubrics group --id <id>
```

> Wire call: `POST /rubric/group`. Run `glow rubrics group --help` for the full flag list.
