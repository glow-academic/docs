# `glow rubrics watch`

# `glow rubrics watch`

Watch Grading rubrics.

## Usage

```bash
glow rubrics watch --id <id>
```

> Wire call: `POST /rubric/watch`. Run `glow rubrics watch --help` for the full flag list.
