# `glow rubrics delete`

# `glow rubrics delete`

Delete Grading rubrics.

## Usage

```bash
glow rubrics delete --id <id>
```

> Wire call: `POST /rubric/delete`. Run `glow rubrics delete --help` for the full flag list.
