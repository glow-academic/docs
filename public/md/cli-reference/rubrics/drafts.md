# `glow rubrics drafts`

# `glow rubrics drafts`

Drafts Grading rubrics.

## Usage

```bash
glow rubrics drafts --id <id>
```

> Wire call: `POST /rubric/drafts`. Run `glow rubrics drafts --help` for the full flag list.
