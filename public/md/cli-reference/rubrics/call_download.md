# `glow rubrics call_download`

# `glow rubrics call_download`

Call Download Grading rubrics.

## Usage

```bash
glow rubrics call_download --id <id>
```

> Wire call: `POST /rubric/call_download`. Run `glow rubrics call_download --help` for the full flag list.
