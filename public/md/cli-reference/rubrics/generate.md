# `glow rubrics generate`

# `glow rubrics generate`

Generate Grading rubrics.

## Usage

```bash
glow rubrics generate --body '\{...\}'
```

> Wire call: `POST /rubric/generate`. Run `glow rubrics generate --help` for the full flag list.
