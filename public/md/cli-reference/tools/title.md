# `glow tools title`

# `glow tools title`

Title Tool integrations.

## Usage

```bash
glow tools title --id <id>
```

> Wire call: `POST /tool/title`. Run `glow tools title --help` for the full flag list.
