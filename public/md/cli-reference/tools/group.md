# `glow tools group`

# `glow tools group`

Group Tool integrations.

## Usage

```bash
glow tools group --id <id>
```

> Wire call: `POST /tool/group`. Run `glow tools group --help` for the full flag list.
