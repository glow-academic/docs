# `glow tools update`

# `glow tools update`

Update Tool integrations.

## Usage

```bash
glow tools update --id <id>
```

> Wire call: `POST /tool/update`. Run `glow tools update --help` for the full flag list.
