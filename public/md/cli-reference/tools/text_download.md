# `glow tools text_download`

# `glow tools text_download`

Text Download Tool integrations.

## Usage

```bash
glow tools text_download --id <id>
```

> Wire call: `POST /tool/text_download`. Run `glow tools text_download --help` for the full flag list.
