# `glow tools draft`

# `glow tools draft`

Draft Tool integrations.

## Usage

```bash
glow tools draft --id <id>
```

> Wire call: `POST /tool/draft`. Run `glow tools draft --help` for the full flag list.
