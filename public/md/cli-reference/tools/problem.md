# `glow tools problem`

# `glow tools problem`

Problem Tool integrations.

## Usage

```bash
glow tools problem --id <id>
```

> Wire call: `POST /tool/problem`. Run `glow tools problem --help` for the full flag list.
