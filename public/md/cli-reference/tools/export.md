# `glow tools export`

# `glow tools export`

Export Tool integrations.

## Usage

```bash
glow tools export --id <id>
```

> Wire call: `POST /tool/export`. Run `glow tools export --help` for the full flag list.
