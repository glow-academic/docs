# `glow ledger sync`

Sync local ledger entries to LearnLoop API for billing

## Usage

```bash
glow ledger sync --path <path>
```

## Options

| Flag | Required | Description |
|---|---|---|
| `--path` | Yes | Path to the ledger directory (env: `GLOW_LEDGER_PATH`) |