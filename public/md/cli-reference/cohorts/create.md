# `glow cohorts create`

# `glow cohorts create`

Create User cohorts.

## Usage

```bash
glow cohorts create --body '\{...\}'
```

> Wire call: `POST /cohort/create`. Run `glow cohorts create --help` for the full flag list.
