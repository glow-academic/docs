# `glow cohorts problem`

# `glow cohorts problem`

Problem User cohorts.

## Usage

```bash
glow cohorts problem --id <id>
```

> Wire call: `POST /cohort/problem`. Run `glow cohorts problem --help` for the full flag list.
