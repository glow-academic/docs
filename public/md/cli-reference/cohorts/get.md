# `glow cohorts get`

# `glow cohorts get`

Get User cohorts.

## Usage

```bash
glow cohorts get --id <id>
```

> Wire call: `POST /cohort/get`. Run `glow cohorts get --help` for the full flag list.
