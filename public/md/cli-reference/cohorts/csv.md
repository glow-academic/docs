# `glow cohorts csv`

# `glow cohorts csv`

Csv User cohorts.

## Usage

```bash
glow cohorts csv --id <id>
```

> Wire call: `POST /cohort/csv`. Run `glow cohorts csv --help` for the full flag list.
