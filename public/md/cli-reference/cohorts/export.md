# `glow cohorts export`

# `glow cohorts export`

Export User cohorts.

## Usage

```bash
glow cohorts export --id <id>
```

> Wire call: `POST /cohort/export`. Run `glow cohorts export --help` for the full flag list.
