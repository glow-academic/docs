# `glow cohorts text_download`

# `glow cohorts text_download`

Text Download User cohorts.

## Usage

```bash
glow cohorts text_download --id <id>
```

> Wire call: `POST /cohort/text_download`. Run `glow cohorts text_download --help` for the full flag list.
