# `glow cohorts draft`

# `glow cohorts draft`

Draft User cohorts.

## Usage

```bash
glow cohorts draft --id <id>
```

> Wire call: `POST /cohort/draft`. Run `glow cohorts draft --help` for the full flag list.
