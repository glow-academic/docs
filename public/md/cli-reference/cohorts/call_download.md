# `glow cohorts call_download`

# `glow cohorts call_download`

Call Download User cohorts.

## Usage

```bash
glow cohorts call_download --id <id>
```

> Wire call: `POST /cohort/call_download`. Run `glow cohorts call_download --help` for the full flag list.
