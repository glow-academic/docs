# `glow cohorts duplicate`

# `glow cohorts duplicate`

Duplicate User cohorts.

## Usage

```bash
glow cohorts duplicate --id <id>
```

> Wire call: `POST /cohort/duplicate`. Run `glow cohorts duplicate --help` for the full flag list.
