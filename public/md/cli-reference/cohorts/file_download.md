# `glow cohorts file_download`

# `glow cohorts file_download`

File Download User cohorts.

## Usage

```bash
glow cohorts file_download --id <id>
```

> Wire call: `POST /cohort/file_download`. Run `glow cohorts file_download --help` for the full flag list.
