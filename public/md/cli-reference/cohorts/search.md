# `glow cohorts search`

# `glow cohorts search`

Search User cohorts.

## Usage

```bash
glow cohorts search
```

> Wire call: `POST /cohort/search`. Run `glow cohorts search --help` for the full flag list.
