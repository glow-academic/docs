# `glow departments csv`

# `glow departments csv`

Csv Organizational departments.

## Usage

```bash
glow departments csv --id <id>
```

> Wire call: `POST /department/csv`. Run `glow departments csv --help` for the full flag list.
