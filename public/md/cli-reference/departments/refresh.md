# `glow departments refresh`

# `glow departments refresh`

Refresh Organizational departments.

## Usage

```bash
glow departments refresh --id <id>
```

> Wire call: `POST /department/refresh`. Run `glow departments refresh --help` for the full flag list.
