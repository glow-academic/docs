# `glow departments group`

# `glow departments group`

Group Organizational departments.

## Usage

```bash
glow departments group --id <id>
```

> Wire call: `POST /department/group`. Run `glow departments group --help` for the full flag list.
