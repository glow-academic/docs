# `glow departments search`

# `glow departments search`

Search Organizational departments.

## Usage

```bash
glow departments search
```

> Wire call: `POST /department/search`. Run `glow departments search --help` for the full flag list.
