# `glow departments draft`

# `glow departments draft`

Draft Organizational departments.

## Usage

```bash
glow departments draft --id <id>
```

> Wire call: `POST /department/draft`. Run `glow departments draft --help` for the full flag list.
