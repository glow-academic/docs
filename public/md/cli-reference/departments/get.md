# `glow departments get`

# `glow departments get`

Get Organizational departments.

## Usage

```bash
glow departments get --id <id>
```

> Wire call: `POST /department/get`. Run `glow departments get --help` for the full flag list.
