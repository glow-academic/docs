# `glow departments generations`

# `glow departments generations`

Generations Organizational departments.

## Usage

```bash
glow departments generations --id <id>
```

> Wire call: `POST /department/generations`. Run `glow departments generations --help` for the full flag list.
