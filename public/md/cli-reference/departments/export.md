# `glow departments export`

# `glow departments export`

Export Organizational departments.

## Usage

```bash
glow departments export --id <id>
```

> Wire call: `POST /department/export`. Run `glow departments export --help` for the full flag list.
