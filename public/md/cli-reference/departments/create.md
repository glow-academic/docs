# `glow departments create`

# `glow departments create`

Create Organizational departments.

## Usage

```bash
glow departments create --body '\{...\}'
```

> Wire call: `POST /department/create`. Run `glow departments create --help` for the full flag list.
