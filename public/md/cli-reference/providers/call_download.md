# `glow providers call_download`

# `glow providers call_download`

Call Download AI provider integrations.

## Usage

```bash
glow providers call_download --id <id>
```

> Wire call: `POST /provider/call_download`. Run `glow providers call_download --help` for the full flag list.
