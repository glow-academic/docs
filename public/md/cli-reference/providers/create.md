# `glow providers create`

# `glow providers create`

Create AI provider integrations.

## Usage

```bash
glow providers create --body '\{...\}'
```

> Wire call: `POST /provider/create`. Run `glow providers create --help` for the full flag list.
