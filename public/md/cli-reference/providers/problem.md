# `glow providers problem`

# `glow providers problem`

Problem AI provider integrations.

## Usage

```bash
glow providers problem --id <id>
```

> Wire call: `POST /provider/problem`. Run `glow providers problem --help` for the full flag list.
