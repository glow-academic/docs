# `glow providers delete`

# `glow providers delete`

Delete AI provider integrations.

## Usage

```bash
glow providers delete --id <id>
```

> Wire call: `POST /provider/delete`. Run `glow providers delete --help` for the full flag list.
